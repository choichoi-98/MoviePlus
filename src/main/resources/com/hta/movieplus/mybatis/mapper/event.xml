<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hta.movieplus.mybatis.mapper.EventMapper">
	
	<insert id="insert" parameterType="event">
		INSERT INTO EVENT
		VALUES((select nvl(max(EVENT_NUM),0)+1 from EVENT),#{EVENT_TYPE}, 
		#{EVENT_SUBJECT),#{EVENT_CONTENT}, SYSDATE, #{EVENT_FILE}, 
		#{EVENT_STARTDATE}, #{EVENT_ENDDATE})
	</insert>	
	
	
	
</mapper>